services:
  mock-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mock-server
    ports:
      # App listens on 10000 by default (see application.properties); map host 8080 for convenience.
      - "8080:10000"
